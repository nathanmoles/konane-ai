Play Konanae vs AI algorithm

RUN INSTRUCTION:
- go into the prject directory in terminal
- run command: python konane.py

If you want to change the alogirthm beign used change the Boolean in the last line of the runGame() method 
- True: minimax with alphabeta pruning
- False: minimax without alphabeta

If you would like to change the depth bounds for either minimax() or alphabeta()
- go the corresponding method and change the depth variable in the first if statement to the desired value
